name: Main CI

on:
  push:
    branches: [ main ]

jobs:
  build-and-test:
    uses: ./.github/workflows/build-and-test.yml
    permissions:
      contents: read
    strategy:
      fail-fast: true
      matrix:
        os: [ ubuntu-22.04, macos-latest ]
    with:
      ref: ${{ github.sha }}
      runs-on: ${{ matrix.os }}
